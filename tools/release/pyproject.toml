[project]
name = "searchhn-release"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "questionary>=2.1.1",
    "rich>=14.3.2",
    "semver>=3.0.4",
    "tomlkit>=0.15.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
